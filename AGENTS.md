When you encounter a new problem that required significant searching to resolve or a developer intervention, add a brief summary and the solution to the end of this file.

This project uses uv; `uv run transcribe`
Use python 3.13 best practices and numpy style docstrings. 
Before adding a new dependency, explain the need and ask the developer.

Guidance from past stumbling blocks:
- Keep non-audio commands free of eager audio/backend imports.
- Windows audio capture on this machine was more reliable with soundcard (native WASAPI mic/loopback enumeration) than sounddevice/PortAudio, which exposed duplicate MME/DirectSound/WDM-KS endpoints and silent streams. Use `transcribe.audio.windows_capture` with `soundcard` for Windows, keep Linux on `sounddevice`, and treat WASAPI `0x80070005` as a Windows microphone privacy/access issue with an explicit remediation message instead of a generic device error.
- soundcard on Windows currently calls NumPy binary romstring() inside soundcard.mediafoundation, which breaks on NumPy 2 with 'The binary mode of fromstring is removed'. Patch soundcard.mediafoundation.numpy.fromstring to delegate binary inputs to 
umpy.frombuffer during backend import so live capture keeps working without waiting on an upstream release.

- Live sessions can begin recording before the ASR model finishes loading. Keep the startup progress order as `capture_ready -> loading_model(capture_active=True) -> model_ready(buffered_audio_sec=...) -> transcribing_started`, and have CLI messaging explain buffering/catch-up so non-debug output stays clear.
- The Tkinter UI runs as one long-lived process. Any workflow that calls `configure_runtime()` permanently installs the outbound network guard for that process, so networked tasks like packaged model install or `init-bench` must run before offline workflows or after restarting `transcribe-ui`.
- The Windows packaged launcher can stay as a single `transcribe.exe`: dispatch to the GUI when started with no args, but keep CLI behavior for any arguments. Keep `transcribe.packaged_ui` explicitly included in standalone build imports so packaged GUI launches are not dropped by the bundler.
- The UI network toggle has two layers: skip the socket guard and relax HF offline env flags. It supports the safe flow of enabling network first, downloading/caching models, then turning network back off; once an offline task installs the socket guard, re-enabling network still requires restarting the UI process.
- The UI network toggle must do more than skip `install_outbound_network_guard()`: HF-backed runtime helpers also pass `local_files_only=True` in live-session and benchmark flows, so they need to derive an effective offline mode from the same process-level network toggle or first-run model downloads will still fail.
- The `Allow Network Access` UI toggle now survives restarts via `data/ui-preferences.json` (or the packaged data root). Persist both the network opt-in and expanded-options state so the required restart flow is actually usable and the app reopens showing that network access is still enabled.
- Windows standalone packaging now persists a package-input fingerprint in `dist/windows/build/package-reuse.json` and reuses the existing packaged stage when the Python/package inputs are unchanged. Leave `--reuse-package` enabled for fast repeat builds, and use `--no-reuse-package` or `--clean` to force a fresh packager run. For local tuning, `--nuitka-jobs` and `--nuitka-lto` pass directly to Nuitka.
- Nuitka standalone builds that dispatch to the Tk GUI need `--enable-plugin=tk-inter`; including `transcribe.packaged_ui` alone is not enough. If `transcribe.exe` exits immediately with a tk-inter plugin error, force a fresh package rebuild after updating the Nuitka command.
- SoundCard on Windows needs its package data in the standalone bundle, not just compiled modules. Include `--include-package-data=soundcard` for Nuitka builds and validate that `soundcard/mediafoundation.py.h` is present before reusing or shipping a staged bundle.
- Nuitka standalone builds need distribution metadata as well as modules/data. Include `--include-distribution-metadata=...` for the app's transitive distributions, and validate report-used metadata like `libcst` so `transformers`/Parakeet runtimes do not fail after install.
- Do not include distribution metadata for every installed wheel. Nuitka rejects metadata-only distributions whose package is not compiled (for example `Mako` without `mako`); seed first-build metadata with known runtime packages, then expand from `nuitka-report.xml` only for distributions whose top-level packages are actually in the compiled module graph.
- When deriving Nuitka `--include-distribution-metadata` from `nuitka-report.xml`, filter out distributions for packages we explicitly nofollow or exclude (for example `matplotlib`). Nuitka treats metadata as belonging to the related package name, so metadata for an excluded package can hard-fail the build even if only helper modules like `pylab` appeared in the report.
- Some distributions advertise junk top-level roots like `examples`, `tests`, or `docs` in their wheel metadata (for example `nemo-toolkit`). Treat those as blockers for `--include-distribution-metadata=...`; otherwise Nuitka can fail the build by insisting on unrelated packages that we do not ship.
- Windows build bootstrap should detect the lowercase `nuitka` module name, not `Nuitka`. `uv pip install nuitka` can succeed and still make `find_spec("Nuitka")` fail, so package checks should use `find_spec("nuitka")` before raising a false "could not be imported" error.
- Do not rely on the `llama.cpp` GitHub `releases/latest` payload alone for Windows CPU runtimes. Scan recent releases and pick the newest matching Windows CPU zip, because the latest release can temporarily expose only CUDA/openEuler assets while the CPU build is still missing from that API response.
- Apply Nuitka metadata safety filters to the seed metadata list as well as report-derived additions. A clean first build has no prior report yet, so unsafe seeds like `nemo-toolkit` can still hard-fail the build before the report-based filter ever runs.
- Inno Setup Pascal Script can be picky about nested multiline calls inside installer code paths. Keep `FailBootstrap(Format(...))` style error reporting on one line in `.iss` code blocks when possible; the multiline form in `new_transcribe_installer.iss` hit a compiler syntax error even though the equivalent single-line call compiled cleanly.
- Do not delete the previous `nuitka-report.xml` before computing the next build's metadata arguments. The report-derived metadata expansion only works if the prior report is still readable, and first-pass seeds should also include safe Hugging Face runtime metadata like `datasets`, `accelerate`, `requests`, `packaging`, `filelock`, `regex`, `tqdm`, `numpy`, and `PyYAML` because `transformers` can query those versions at runtime even when the packages themselves are not followed for compilation.
- The simplified Windows builder in `scripts/new_build_windows_standalone.py` should bootstrap missing developer prerequisites instead of hard-failing: use `uv sync --extra nemo-asr --inexact` (or pip editable install fallback) for missing repo/runtime deps, `uv pip install --python <active-python> nuitka` (or pip fallback) for Nuitka, and a local cached Inno Setup install under `dist/wbuild/_cache/tools/inno-setup` when `ISCC.exe` is not already available.
- If Nuitka errors with "including metadata for distribution 'PyYAML' without including related package '_yaml'", keep the `PyYAML` metadata but add `--include-module=_yaml` when the extension is installed. `PyYAML` advertises `_yaml` as a related top-level module, so metadata-only inclusion is not enough.
- The simplified Windows builder's Nuitka metadata filter must reject metadata for top-level packages we explicitly `nofollow` or exclude, such as `_pytest`, `coverage`, `mypy`, `pytest`, and `setuptools`; otherwise clean builds can fail with metadata-only errors for packages we intentionally do not compile.
- `transformers` in the packaged Parakeet runtime may require `datasets` distribution metadata. Treat `datasets` as an intentional packaged dependency in the simplified Windows builder: include the `datasets` package in the Nuitka build and keep its metadata enabled.
- `datasets` in the packaged Parakeet runtime eagerly depends on `pyarrow` and reads its installed distribution version at import time. In the simplified Windows builder, treat `pyarrow` as an intentional packaged dependency too: include the `pyarrow` package in the Nuitka build, keep its distribution metadata enabled, and do not leave it on the Nuitka `nofollow` exclusion list.
- `import nemo.collections.asr` eagerly loads a broad runtime stack in this environment, including `hydra`, `lightning`, `pytorch_lightning`, `torchmetrics`, `librosa`, `scipy`, `sentencepiece`, `jiwer`, `editdistance`, `lhotse`, `webdataset`, `wget`, `soundfile`, `pydub`, `pandas`, `pyannote`, `matplotlib`, `IPython`, `datasets`, `pyarrow`, and `transformers`. The simplified Windows builder should explicitly include those runtime package roots and must not `nofollow` `matplotlib` or `IPython`, or packaged NeMo/Parakeet startup can fail after install.
- explicitly include `transformers.commands` if the packaged runtime needs the Hugging Face CLI helpers. `transformers.commands.add_new_model_like` can hit a Nuitka transformation bug around its generated `__init__` f-string block, so keep the commands package included and patch just that module through the custom Nuitka package-config file instead of `nofollow`ing the whole `transformers.commands` package.
